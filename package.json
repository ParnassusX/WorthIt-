{
  "name": "worthit",
  "version": "1.0.0",
  "description": "WorthIt! - Product value analysis tool",
  "main": "index.js",
  "scripts": {
    "start": "cd web-app && npm run dev",
    "build": "cd web-app && npm run build",
    "deploy": "netlify deploy --prod --memory=2048",
    "deploy:netlify": "cd web-app && npm run build && netlify deploy --prod --memory=2048",
    "postinstall": "cd web-app && npm install --prefer-offline --no-audit --progress=false --no-fund"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "workspaces": [
    "web-app"
  ]
}